# plan-review findings log — opencode-optimisations

Dated entries appended by the `plan-review` agent, one per refined TODO.md item.
(Fresh log for this repo — the agent was copied from the `admin` toolkit; admin's
own findings history stays in that repo.)

---

## TODO item 16 — Full-harness trace-driven fixes (round 2) (2026-06-23)

**Ambiguities found**: 6

| Category | Finding | Resolution |
|---|---|---|
| Constraint compliance | TODO marked timeout-30→10 and progress-heartbeat as landed `[x]`, but `harness_eval.py` still has `DEFAULT_INSTANCE_TIMEOUT = 30*60` and no heartbeat — they landed in `admin` pre-extraction | Re-implement fresh here as E1, E2 (user-confirmed); corrected the false `[x]` boxes |
| Integration wiring | The config `sampling` block is not referenced anywhere in `harness_eval.py` — L2 `low-temp` may never have applied; L1 has no forwarding path | Added E-sampling prerequisite: wire `sampling` → opencode model options → MLX `/v1`; verify endpoint honours `repetition_penalty` |
| Measurement & signal | Full harness is 0/8 (no gradient) — can't judge L1–L5 on pass/fail | Add episode metrics now (E0): degenerate-loop rate, fraction-of-budget-to-first-tool-call, steps-to-first-edit, tool-call validity (user-confirmed) |
| Constraint compliance | Is L1 `repetition_penalty` an allowed sampling lever or a forbidden engine change? | Allowed — item-11 category-7 sampling param, opencode-side per request (user-confirmed) |
| Adopt/reject criterion | No success bar per lever | Per-lever primary metric ↑ vs baseline AND tool-call validity not regressed; pass/fail secondary (default — not user-confirmed) |
| Definition of done | Vague "done" for item 16 | All 5 levers have adopt/reject decisions AND adopted bundle yields ≥1 full-harness pass (default — not user-confirmed) |

**Tasks added**: E0 (episode metrics), E1/E2 (re-implement timeout + heartbeat), E-sampling (plumbing fix + endpoint verify), L3/L4 feasibility (edit is built-in → shadow/hook), L5 native-`doom_loop`-vs-custom evaluation, measurement plan, docs.
**Tasks removed/changed**: the two `[x]` "already landed" boxes → reopened as E1/E2; "L1 at the MLX/proxy layer" → specified as opencode-side sampling param.
**Documentation changes**: `docs/opencode-local.md` (update — metrics schema + results); `docs/harness-engineering-research.md` (update — L1 cross-link).
**Key design constraint**: levers are measured on per-episode intermediate signals (not 0/8 pass-rate), and L1 anti-repetition is a permitted opencode-side sampling lever — but its plumbing (`sampling` block → MLX endpoint) does not yet exist and must be built first.

---

## TODO item 19 — Structured prompt-optimisation (GEPA) (2026-06-24)

**Ambiguities found**: 9

| Category | Finding | Resolution |
|---|---|---|
| Constraint compliance | Item-19 gate ("non-zero, non-degenerate signal") was undefined against item-16's stable 0/8 T3/T4 capability wall — only synthetic T2 has headroom | Top-level checkable PRECONDITION: BLOCKED until item-16 **L5** has a recorded adopt/reject verdict in `TODO.md` AND the T2 gate-check passes (user-confirmed) |
| Measurement & signal | Which tier/fitness signal to optimise on, given T1 ~saturated, T2 has headroom, T3/T4 = 0/8 | `score = T2_frac − λ·penalty` from `tier-report.jsonl`; **T2-only** climbs, **T1 hard gate**, T3/T4 reported weight 0 (user-confirmed) |
| Adopt/reject criterion | No "climbable gradient" threshold to unlock GEPA vs noise | Unlock iff `T2_mean` inside `(floor, ceiling)` AND `(1.0 − T2_mean) > K-run spread`; else "no climbable signal yet" (user-confirmed) |
| Constraint compliance | Tool-call floor protection — penalty term & coefficient undefined | Penalty = rise above baseline in `no-edit + error + catastrophic-edit`; **λ large** so any floor regression ⇒ negative score (can't be bought back) (user-confirmed) |
| Constraint compliance | Offline-at-serve vs the lit "use a stronger reflector" recommendation | Cloud/larger model = **reflector/proposer ONLY**, loop-only, text-levers-only, "serving-offline" assertion; local Gemma stays optimisee+evaluator (user-confirmed) |
| Constraint compliance | Does an online reflector contaminate the adopted result? | **Mandatory offline re-validation**: rerun adopted candidate reflector-disconnected; win survives iff T2 stays within K-run spread AND floor holds — adopted text must stand alone (user-confirmed) |
| Cost & feasibility | "Few rollouts" is RL-relative (hundreds–thousands) × K≥3 at 8–12 tok/s — budget unsized | **T2-only budget** `≤N × K=3`; N + abort wall-clock ceiling computed in 19.2 from a measured per-T2-rollout timing micro-task; no T3/T4 GEPA until the wall moves (user-confirmed) |
| Experiment design | Counter-arm to validate item-16's NEGATIVE claim ("prompt changes don't help here") was vague | Counter-arm = **single fixed GEPA candidate vs frozen baseline, K≥3** — does optimisation move T2 at all (user-confirmed) |
| Integration wiring | Where CAPO/OPRO fallback sits now that budget is tier-scoped; new dependency vs offline constraint | Fallback = same T2 scalar + λ + K≥3, swap optimiser only (`promptolution`, offline-native), triggered on GEPA budget-abort; deps are online-install at setup only (user-confirmed) |

**Tasks added**: top-level L5+gate precondition; 19.2 split into gate-check / per-T2-rollout timing micro-task / reflector-wiring confirm / fitness-read confirm; 19.3 split into fitness+λ+T1-gate / cloud-reflector-loop+T2-budget / counter-arm / offline re-validation / fallback / valid-outcomes / `make check`; a Documentation sub-section (4 docs).
**Tasks removed/changed**: 19.2 (a/b/c prose) → concrete sub-tasks incl. the timing deliverable; 19.3 (single "prototype + penalise regressions") → the full resolved design with explicit fitness scalar, gate, counter-arm, offline re-val, and budget abort path.
**Documentation changes**: `docs/structured-optimisation-research.md` (update — resolved design + eventual local result replacing [lit-only]); `docs/tiered-harness.md` (update — fitness-read + scalar definition); `docs/opencode-local.md` (update — lever outcome on close); `CHANGELOG.md` (update — on closed outcome).
**Key design constraint**: GEPA stays gated behind item-16 L5 + a measurable T2 climbable gradient; it optimises a T2-only `T2_frac − λ·penalty` scalar with a large λ and a T1 hard gate so a pass-rate gain can never buy back a tool-call regression; a cloud reflector is allowed in the (online-optional) loop only, but the adopted text must survive a fully-offline reflector-disconnected re-validation. The 19.1 GEPA verdict remains **[lit-only]** until this local run closes it.

---

## TODO item 18 — Improvement-recommender agent (2026-06-24)

**Ambiguities found**: 7 (4 resolved from repo audit, 3 resolved by the user 2026-06-24);
**+1 build-form revision 2026-06-24** — the user later split item 18 into a two-layer
shape (deterministic Python evidence digest → Claude Code / Opus-4.8 proposer),
superseding the original single deterministic `recommend` subcommand.

| Category | Finding | Resolution |
|---|---|---|
| Integration wiring | 18.1's stated input "Jaeger traces" is the wrong source: Jaeger all-in-one is in-memory/ephemeral (cleared on stop), opt-in (`MLX_OTEL=1` + sourced env), and its spans carry no per-token text for degenerate-loop detection | Switch input to the **durable on-disk corpus** — `runs/<run>/<instance>/opencode.jsonl` (80+ files present) + `ledger.jsonl` + `tier-report.jsonl`, all already structured by `parse_episode_jsonl`/`classify_failure`. Jaeger demoted to live human-debugging aid. |
| Scope gaps | 18.1 claimed "claude-mem observations" as an input, but no `.claude-mem/` store exists in this repo — the only "memory" is the Claude Code auto-memory (mem-search), not a programmatic feed | Removed claude-mem as a programmatic input; prior-work context = `docs/*-research.md` + TODO history read as text |
| Measurement & signal | No reuse path stated for the shared failure taxonomy | Reuse `classify_failure`/`FAILURE_CATEGORIES` (item-16 7-mode + 3-outcome); no new enum |
| Output / recommendation surface | 18.2 "suggests skills/hooks/prompts" — concreteness undefined (free-text vs runnable) | Each recommendation must **emit a runnable `harness_configs/*.json`** (existing schema) so 18.3 A/Bs it with no hand-translation; free-text-only rejected. **(user-confirmed)** Lever concreteness: schema-expressible levers → runnable config; defects needing new code → flagged `needs-implementation` note (NOT a runnable config), kept out of the auto-A/B path |
| Build form | Whether item 18 is a CLI subcommand, standalone script, or LLM agent was unspecified | **(user-confirmed, then user-REVISED 2026-06-24 to a two-layer split)** **Layer 1** = deterministic `harness_eval.py recommend` subcommand (reuses parser/ledger/`parse_episode_jsonl`/`classify_failure`/`make check`) that emits an **evidence digest** only — does not rank or invent levers. **Layer 2** = a **Claude Code agent on Opus 4.8** as the **proposer**, consuming the digest + prior-work docs to emit ranked recommendations. The earlier "no `.claude/agents/` LLM agent" rejection is **explicitly superseded** — the user chose the LLM proposer (validated, not unit-tested). Still rejected: standalone `scripts/recommend.py`; a *purely* deterministic ranker (can't surface novel/cross-mode levers) |
| Adopt/reject criterion (validation) | The user's explicit "how do we know the recommender is good?" — undefined | **(user-confirmed)** known-answer backtest scored on **recall AND precision** vs the 7-mode taxonomy: build a labelled ground-truth set from the pre-fix corpus, require the Opus-4.8 proposer to surface the hand-found defects on their instances (recall) AND penalise spurious flags (precision) — flagging everything fails. Because the proposer is **non-deterministic**, score **several samples** and require the bar on the **majority** (report spread, mirrors item-16 K-run discipline); only **Layer 1** is unit-tested. **Decisive** gate stays 18.3: emitted config moves a tier/mode at K≥3 |
| Constraint compliance (offline-at-serve) | Does the Opus-4.8 proposer violate the fully-offline-at-serve constraint? | **No** — Layer 2 calls Opus 4.8 in the **analysis/optimisation loop**, not the frozen local serve path; mirrors item 19's cloud-reflector-loop-only carve-out ("serving stays offline; the optimisation loop may be online"). Only 18.3's A/B re-runs the local Gemma/MLX model |
| Cost & feasibility / sequencing | Whether item 18 is gated behind item 16 like 19/20/21.4c | **Not gated** — it is offline analysis over existing artifacts, runnable now; only its OUTPUT is bound by the Evidence policy (each proposal `[tool-proposed]` until a local A/B closes it) |

**Tasks added**: 18.0 known-answer backtest (recall+precision, majority-over-samples validation prereq); 18.1 = Layer-1 episode-corpus ingestion/digest (no Jaeger); 18.2 = Layer-2 Claude Code / Opus-4.8 proposer (config-emitting ranked report + `needs-implementation` split + schema-validation of LLM output); 18.3 (K≥3 close-the-loop); a `make check` task scoped to Layer 1; Measurement plan + Documentation sub-sections (4 docs).
**Tasks removed/changed**: "Jaeger trace-ingestion" → on-disk jsonl/ledger ingestion; "claude-mem observations" input dropped; 18.2 free-text → runnable-config emission (code-requiring levers flagged, not emitted); single deterministic `recommend` tool → two-layer split (deterministic digest + Opus-4.8 proposer).
**Documentation changes**: `docs/opencode-local.md` (update — two-layer recommender, corpus input, config output); `docs/tiered-harness.md` (update — Layer 1 consumes tier-report + emits configs); `docs/jaeger-tracing.md` (update — Jaeger is live-debug, not the recommender's source); `CHANGELOG.md` (update — on closed outcome).
**Key design constraint**: Item 18 is a **two-layer recommender** — a deterministic **`harness_eval.py recommend`** Layer 1 that ingests the **already-captured durable episode corpus** (`opencode.jsonl` + `ledger.jsonl` + `tier-report.jsonl`, NOT ephemeral Jaeger spans or a non-existent claude-mem feed) and emits a grounded **evidence digest**, feeding a **Claude Code / Opus-4.8 proposer** (Layer 2) that emits **runnable item-17 configs** for schema-expressible levers (free text rejected; code-requiring fixes become flagged `needs-implementation` notes, schema-validated before they count). The proposer is **validated by a recall-AND-precision known-answer backtest** over several samples (majority bar — it is non-deterministic, so only Layer 1 is unit-tested), and every proposal stays **[tool-proposed]** until a local K≥3 A/B closes it. The Opus-4.8 call sits in the **analysis loop, not the frozen offline serve path** (mirrors item 19's cloud-reflector carve-out). All build/validation decisions are user-confirmed (2026-06-24, build form revised same day); no open questions remain.

---

## TODO item 22 — Online-model harness-soundness control (BigPickle) (2026-06-24)

**Ambiguities found**: 8 (all user-confirmed over two question batches)

| Category | Finding | Resolution |
|---|---|---|
| Integration wiring / Constraint compliance | 22.1 framed the gate as suppressing only the `mlx-local` provider block, but code reading of `scripts/harness_eval.py` found the local stack wired in at **three coupled points**: `apply_levers` (≈L348-389 provider block + `baseURL` + model/small_model), `cmd_run` (≈L1595 `server_healthy` → restart/abort `return 2`), and `detect_model` (≈L1604 queries live MLX `/v1/models`) + the `_score_subset` OOM-restart (≈L1663). A provider-block-only gate would still abort with MLX off. | **Gate ALL THREE** (user-confirmed): `external_provider` skips the provider block AND health-check/restart AND `detect_model`/OOM-restart; `model_ref` comes from config/`--model` only, so the run works with **MLX fully off**. |
| Tool-call reliability / Integration wiring | Removing `server_healthy` removes the only pre-loop guard; a missing/expired `opencode auth login` or no network would otherwise fail opaquely mid-episode across all 8 instances. | Add a **one-shot auth + connectivity pre-flight** when `external_provider` is on (auth-status + trivial `opencode run` ping); abort early with `run 'opencode auth login' + check network` (user-confirmed). It replaces the removed MLX health-check. |
| Adopt/reject criterion | 22.3's "≈0/8 ⇒ broken / ≫0/8 ⇒ sound" was unquantified and had no middle-zone action. | **Banded verdict** (user-confirmed): **≤1/8 broken**, **≥5/8 sound**, **2–4/8 inconclusive ⇒ 22.5** (new conditional sub-item). |
| Measurement & signal | "Does the 7-mode taxonomy apply to an online model?" — code read shows `FAILURE_CATEGORIES`/`classify_failure` (≈L456-506) is **provider-agnostic** (derived from terminal `reason` + E0 metrics, not model identity), so BigPickle reuses the same 10-category vocabulary unchanged. But several mechanical modes (`oom`, mlx-tuned `degenerate-loop`) structurally cannot fire for a competent online model. | **Histogram is PRIMARY evidence, pass-rate secondary** (user-confirmed): the "sound" signature is BigPickle landing mostly in `ok`/`tests-failed` (capability modes) with **ZERO** `oom`/`degenerate-loop`/`no-edit`/`edit-mismatch` (mechanical modes); 22.4 frames the verdict on this, not just the aggregate. |
| Experiment design | "Identical" was undefined — literal byte-identity would force Gemma's 600s timeout (≈L103, tuned for ~8-12 tok/s) and mlx-only sampling keys onto a fast gateway model. | **Hold tools+prompt+subset+scoring identical; ALLOW provider-appropriate sampling/context + shorter timeout**, with every delta recorded in the ledger `notes` (user-confirmed). |
| Cost & feasibility / Integration wiring | `make harness-eval` doc says "Needs the stack up (make mlx-up)" (Makefile ≈L71) — misleading once the online arm skips MLX. | Add a **dedicated `make harness-eval-online CONFIG=...`** target (no `mlx-up` dep; documents network + one-time `opencode auth login`); fix the `harness-eval` comment to note the online exception (user-confirmed). |
| Inconclusive action / Measurement & signal | The 2–4/8 band needed a concrete artifact and a way to separate a real bug from gateway variance. | **22.5 (conditional)**: manually read one passing + one failing BigPickle trace from the already-saved `run_dir/opencode.jsonl` + `opencode.log`, AND re-run the 2–4/8 instances at **higher K** to distinguish a mechanical bug from gateway-side nondeterminism before the verdict (user-confirmed). |
| Definition of done / Documentation | No `make check`/regression gate on the 22.1 harness change; no doc surface listed. | Add a **`selftest` assertion** (with `external_provider` on, the written `opencode.json` has no `mlx-local`/`DEFAULT_PROVIDER` block and no local `baseURL`, and model/small_model equal the external ref) + `make check`; add a Documentation sub-section (5 surfaces). |

**Tasks added**: rewrote 22.1 as gate-all-three + selftest + `make check`; folded auth/connectivity pre-flight into 22.2; added histogram-signature framing to 22.4; added **22.5** (conditional inconclusive-disambiguation: trace-read + higher-K re-run); added a `### Design decisions (resolved)` block and a `### Documentation` block.
**Tasks removed/changed**: 22.1 "suppress provider block only" → gate all three local-only assumptions; 22.3 "≈0/8 vs ≫0/8" → banded ≤1/≥5/2–4 verdict; 22.3 "identical" → identical-where-it-proves-soundness with recorded deltas; "7-mode histogram" wording corrected to the provider-agnostic 10-category taxonomy.
**Documentation changes**: `docs/opencode-local.md` (update — gate + online arm + verdict + side-by-side histograms); `CHANGELOG.md` (update — 22.4 verdict); `Makefile` (update — new `harness-eval-online` target + fix the `harness-eval` mlx-up comment); `docs/opencode-config.md` (update — `external_provider` provider-block-omission path); `scripts/harness_configs/online-bigpickle.json` (create — config whose `description` documents held-vs-varied deltas).
**Key design constraint**: Item 22 is a **one-shot online diagnostic control, never a serve-path change** — the frozen Gemma-4-E4B/mlx-lm stack and the offline-at-serve constraint are untouched; the online BigPickle arm runs **solely to validate the harness scaffolding**. Its enabling code change (`external_provider`) must gate **all three** local-only couplings (provider block + MLX health-check/restart + `detect_model`/OOM-restart) so the control runs with MLX fully off, behind a one-shot auth/connectivity pre-flight. The verdict is **banded** (≤1/8 broken, ≥5/8 sound, 2–4/8 inconclusive→trace-read + higher-K re-run) and read **primarily off the provider-agnostic failure histogram** — the "sound" signature is BigPickle in capability modes (`ok`/`tests-failed`) with ZERO mechanical modes — not off the raw pass-rate. All decisions user-confirmed (2026-06-24, two batches); no open questions remain.

---

## TODO item 23 — GEPA on the next rung: shaped-reward T3 (single-file real fixes) (2026-06-26)

**Ambiguities found**: 11 (all user-confirmed over two question batches)

| Category | Finding | Resolution |
|---|---|---|
| Measurement & signal (gate ceiling) | The 19.2 unlock rule `(ceiling − mean) > spread` was reused with no ceiling defined; for binary T2 it was 1.0, but on T3 the F2P-flip is capability-bound, so a 1.0 ceiling over-states the headroom a *text lever* can climb. | **Two ceilings** (user-confirmed): unlock the climb on **ceiling 0.50** (the behavioural ceiling — every instance edits with P2P intact), keep **1.0 as the separate adopt gate**, report both. |
| Experiment design (reward coherence) | The proposed ladder was non-monotone and double-penalised: catastrophic-edit at 0.10 sat *above* no-tool-stop at 0 ("break working code" out-scored "don't start") AND was hit again by a separate aggregate λ term. | **Catastrophic = strictly −0.25** in the per-instance score and **DROP the separate λ term** (user-confirmed) — one coherent penalty, strictly below honest non-engagement, no double-count. |
| Measurement & signal (metric gap) | The ladder needs to split no-tool-stop (0.0) from tool-churn (0.25), but **both classify as `no-edit`** in the item-17 taxonomy; the only discriminator is `tool_call_rounds` (0 vs 8), which the TODO's named E0 metric list (`made_edit`/`pass_to_pass_*`/`fail_to_pass_*`/`reason`) **omitted**. | Add **`tool_call_rounds` to 23.1's named inputs**; `tool_call_rounds >= 1` → 0.25, else 0.0 (user-confirmed). |
| Constraint compliance (hard gate after λ removal) | Removing the λ term left `gepa_fitness` (`T2_frac − λ·floor_rise` + T1 gate) without an enforced floor, and item 23 also needs the adopted **T2 0.917 win** protected. | **Rework `gepa_fitness`**: `score = T3_shaped_mean` (no λ) with **T1 AND T2 BOTH hard gates** — reject the candidate outright if either drops below baseline (user-confirmed). |
| Measurement & signal (total-function gap) | The ladder named only the 3 observed modes + F2P-flip, but a per-instance scorer must be **total** over every terminal; unplaced cases included 21614's **clean-edit-but-timeout** and the `oom`/`error`/`degenerate-loop`/`apply-failed` family. | Key off the edit-based metrics, **ignore terminal `reason` for the edit rungs** (21614's timeout-with-clean-edit = 0.50), and score **hard-fail terminals (`oom`/`error`) = −0.25** (user-confirmed); 23.1 ships a totality selftest covering every terminal → rung. |
| Cost & feasibility / Scope (corpus size) | The T3 tier is **exactly 3 sympy instances**, so the shaped mean is over 3 discrete rungs — too coarse a gradient, and the K-run spread can swamp the 0.50-ceiling headroom (item-19's T2 had 6 instances). | **Expand the T3 tier to ~6** (user-confirmed): mine ~3 more single-file/single-hunk/single-F2P fixes; fall back to 3 + coarse-signal caveat if 3 qualifying offline instances can't be sourced/re-baselined in budget. |
| Constraint compliance (offline corpus mining) | Expanding the corpus risks pulling instances over the network, violating offline-at-serve / offline-loop discipline. | Mine the 3 new instances **OFFLINE from the already-downloaded SWE-bench corpus**, same selection criteria (user-confirmed). |
| Experiment design (frozen-baseline reconciliation) | "Frozen baseline kept throughout" collided with expanding the subset — the old 0/3 baseline doesn't cover new instances, and items 17/19 already logged 3-instance T3 numbers. | The **6-instance set becomes a NEW item-23 frozen baseline** (re-measure baseline K-runs on all 6); the old 3-instance T3 numbers stay **historical**, not comparable (user-confirmed). |
| Integration wiring (lever-channel mismatch) | Scenario (d) said "transfer cand2 — config already exists", but cand2's adopted text is a **`rules.content` lever only the micro suite reads**; the full harness (`apply_levers`) consumes `system_prompt` → `AGENTS.md` and has **no `rules` channel** — "the config already exists" was false for a T3 run. | **Port cand2's terse text into a new `harness_configs/*.json` as an APPEND** to the opencode default (replicate the micro `rules` append, apples-to-apples with item 19; if the `system_prompt`→`AGENTS.md` path replaces rather than appends, 23.2 adds an append channel) (user-confirmed). |
| Cost & feasibility (run discipline) | The item framed an N≈8–12 / 12–18h "longer run" up front, yet 19.3 converged at N=2 — pre-committing 12–18h on a possibly-flat signal is a feasibility risk. | **Two-phase go/no-go** (user-confirmed): a cheap **N≈3 probe** gated by the 23.1 shaped gate-check; **only unlock the longer N≈8–12 run if the probe clears spread on the 0.50 ceiling**. |
| Integration wiring (chunked resume) | The long run spans suspend-prone sessions with an in-loop Opus-4.8 reflector, but no checkpoint/resume granularity was defined. | **Resume at candidate boundaries** (user-confirmed): each candidate's K rollouts complete in one session; persisted state = JSONL ledger + saved candidate configs + a small frontier/tried file the reflector reloads; a mid-candidate suspend discards that candidate's partial rollouts. |

**Tasks added**: split 23.1 into corpus-expand+re-baseline / total shaped per-instance score (+`tool_call_rounds`, +totality selftest) / `gepa_fitness` rework (T1+T2 hard gates, no λ) / budget+two-ceiling gate-check; rewrote 23.2 with the cand2 append-port note; split 23.3 into Phase-1 probe / Phase-2 candidate-boundary-resumed run; expanded 23.4 partial-outcome criterion to "T1+T2 hard gates held"; widened the final `make check` task to totality + two-gate + two-ceiling selftests.
**Tasks removed/changed**: the shaped-ladder rung values + ordering (catastrophic 0.10 → −0.25, dropped the aggregate λ term, added the `oom`/`error`=−0.25 rung and the explicit `tool_call_rounds` split); "Design decisions to resolve" → "resolved"; single-ceiling gate-check → two-ceiling; 3-instance frozen baseline → new 6-instance baseline; scenario (d) "config already exists" → lever-channel-port-as-append; single "longer run" → two-phase go/no-go.
**Documentation changes**: `docs/structured-optimisation-research.md` (update — append §23: shaped T3 reward + two-ceiling gate + longer-run result); `docs/tiered-harness.md` (update — shaped T3 reward as the climbing signal vs the binary tier pass for adopt); `docs/opencode-local.md` + `CHANGELOG.md` (update — only on close). (No change to the existing item-23 Documentation list — already correct.)
**Key design constraint**: Item 23 tests whether a **dense, TOTAL shaped reward** exposes a *climbable sub-signal* under the flat binary T3 0/3 — **without assuming the capability wall moved** (a clean "no movement even under shaping" is a valid wall-confirming close). The reward is a total per-instance function over `made_edit`/`pass_to_pass_*`/`fail_to_pass_*`/**`tool_call_rounds`**/`reason` with rungs **−0.25** (catastrophic-edit or `oom`/`error`, strictly below non-engagement) **< 0.0** (no-tool-stop) **< +0.25** (tool-churn) **< +0.50** (clean P2P-intact edit, timeout non-capping) **< +1.0** (F2P flip); the catastrophic penalty is **baked into the score, not an aggregate λ term**. Fitness = `T3_shaped_mean` with **T1 AND T2 both hard gates** (the adopted T2 0.917 win is protected). The climb unlocks on a **0.50 behavioural ceiling** (text-lever-reachable) while **1.0 (binary F2P flip) stays the separate adopt gate**. The T3 tier is **expanded to a new 6-instance offline-mined frozen baseline** for a finer gradient, and the run is **two-phase go/no-go** (cheap N≈3 probe → longer N≈8–12 only if it clears spread), chunked with **candidate-boundary resume**. Serving stays frozen/offline; the Opus-4.8 reflector is loop-only. All decisions user-confirmed (2026-06-26, two batches); no open questions remain.

---

## TODO item 20 — Planning-first phase / orchestration topology (2026-06-27)

**Ambiguities found**: 8 (4 decided by the user via the coordinator relay; 4 resolved
with repo-grounded defaults). **Decisions reached the user in relay mode** (this agent ran
as a subagent without `AskUserQuestion`); answers were relayed back through the coordinator
and are treated as the working spec — not direct user confirmation. The 20.1 verdict stays
**[lit-only]** until 20.3 measures it locally.

| Category | Finding | Resolution |
|---|---|---|
| Measurement & signal (broken primary) | 20.3's stated primary metric — **degenerate-loop rate** — is a non-bottleneck (item 16: real modes are no-tool-stop + tool-churn, not loops), and full-harness pass-rate is the flat **0/8** capability wall with no gradient. No dense signal was defined. | **Reuse item-23's shaped per-instance reward** (`gepa_t3_shaped_score`, rungs −0.25/0/+0.25/+0.50/+1.0), reported as the subset mean (relay-chosen "most rigorous"). Degenerate-loop rate **demoted to secondary**. |
| Measurement & signal (signal-vs-subset mismatch) | The shaped reward is keyed on `pass_to_pass_*`/`fail_to_pass_*` → it only scores **T3/T4 real-fix** instances cleanly; the full subset's T1/T2 micro rungs have no P2P/F2P to map, so the chosen primary is undefined on most of the original subset. | **Run 20.3 on the item-23 6-instance T3 set** (same shaped-reward regime) so the primary is well-defined for every arm; full pass/8 stays the separate binary adopt gate. The 23.1 bare-baseline numbers (mean 0.153, K=3) are reused, not re-measured. |
| Cost & feasibility (unverified arm feasibility) | Arms (b) native Plan→Build and (c) sub-agent ride **raw in `opencode_config`** — the schema (`load_config`/`apply_levers`) has **no first-class lever** for opencode's Plan/Build agents or the `task` tool, and it is unproven the weak Gemma can drive them or emit valid tool-calls under them. | User chose to **run all three arms now** (overriding gate-then-expand). A **build-time feasibility precondition** (1–2 instance smoke) is folded into **20.2**: an arm that can't emit valid tool-calls is recorded as a **wall-confirming null**, never silently skipped. |
| Cost & feasibility (no budget/abort discipline) | Item 20 had no K, wall-clock ceiling, gate, or fallback for the **8–15× token** multi-agent arm on a 16 GB M1. | Adopt the **item-23 pattern** (default): K≥3 mean, wall-clock ceiling sized from the measured ~257 s T3 rollout median via `gepa_budget`, a **go/no-go gate before arm (c)**, **abort → fallback to arm (a)**. |
| Integration wiring (stale lever channel) | The goal-plan template was implied to inject via `system_prompt` (REPLACE), but item 18 proved REPLACE **suppresses tool use** on the weak 4B. | Inject via **`rules_append` (APPEND → `AGENTS.md`)**, confirmed live in `apply_levers` (`harness_eval.py:446-458`). Both the cand2 rules and the goal-nudge ride it. |
| Experiment design (baseline confound) | Unspecified whether planning arms sit on bare baseline or item-19's adopted cand2 (now portable via `rules_append`). | **Both reference arms** run (relay-chosen): bare default (= 23.1 floor) + default+cand2 (shipping config); **all topology arms sit on the cand2 base** so topology is the single varying lever. |
| Experiment design (thinking unoperationalized) | "Planner thinks / executor thin" (finding #2) had no mechanism, and arm (a) is a single rollout with no separable executor. | Arm (a) = **bounded goal-plan nudge via `rules_append` + `nothink` sampling**; the single-pass no-hard-split **tension is documented, not hidden** (relay-chosen). Arms (b)/(c) concentrate thinking in the plan role. |
| Structural (circular boundary + missing subsections) | 20.2/20.3 boundary was circular (both implied building + running), and item 20 lacked the Measurement-plan + Documentation subsections that 18/19/23 have. | **20.2 = pick shape + build configs + feasibility smoke (NO run); 20.3 = run the A/B** (default). Added a `### Design decisions (resolved)`, `### Measurement plan`, and `### Documentation` subsection. |

**Tasks added**: a `### Design decisions (resolved)` block (8 decisions); rewrote 20.2 as build-configs-only (5 named arm configs + the feasibility-smoke precondition + the goal-not-how-to rule, no run); rewrote 20.3 as the 5-arm A/B on the T3 set with the shaped primary + two-ceiling adopt gate + go/no-go-before-(c) + abort→fallback + the three valid closed outcomes; added a `### Measurement plan` and `### Documentation` (4 docs) subsection; added a `make check` + selftest gate.
**Tasks removed/changed**: 20.2's "cheapest-first (a)/(b)/(c)" prose → concrete named configs with a build-time feasibility gate; 20.3's **degenerate-loop-rate primary** → item-23 shaped-reward primary (loop-rate demoted to secondary); 20.3's "ALL arms on the E0 subset" → all arms on the **item-23 6-instance T3 set**; the single "baseline" arm → **two reference arms** (bare + cand2); injection channel pinned to `rules_append` (not `system_prompt`).
**Documentation changes**: `docs/orchestration-planning-research.md` (update — append a "20.3 local validation" section converting the [lit-only] verdict to the measured result); `docs/tiered-harness.md` (update — item 20 reuses the shaped T3 reward + `rules_append` topology arms); `docs/opencode-local.md` (update — record the adopt/reject/partial outcome on close); `CHANGELOG.md` (update — on close).
**Key design constraint**: Item 20 is a **5-arm orchestration-topology A/B** measured with item-23's **shaped per-instance reward** (the dense climbing signal — the originally-specified degenerate-loop-rate primary is a **non-bottleneck** and is demoted) over the **shared item-23 6-instance T3 real-fix set** (the only subset where the P2P/F2P-keyed reward is well-defined for every arm), with **full pass/8 (binary F2P flip) as the separate two-ceiling adopt gate**. The single varying lever is the **topology** (flat ReAct → single-pass goal-plan → Plan/Build → multi-agent), held on the **cand2 `rules_append` base** with bare + cand2 as the two reference brackets; the plan nudge injects via **`rules_append` (APPEND, never `system_prompt` REPLACE — item-18's tool-suppression regression)** and arm (a) approximates "thin executor" via a goal-nudge + `nothink` (single-pass split tension documented). All three arms run now (user override of gate-then-expand), but each structural arm carries a **build-time feasibility smoke** — an arm Gemma can't drive is a **recorded wall-confirming null, not a skip** — and the **8–15× multi-agent arm sits behind a `gepa_budget`-sized go/no-go gate with abort→fallback to arm (a)**; `gepa_assert_serving_offline` guards every arm. The 20.1 planning verdict stays **[lit-only]** until 20.3 closes it locally. Decisions relayed via the coordinator (2026-06-27); not direct user confirmation.

---

## TODO item 27 — Extend GEPA to optimise ONLINE optimisee models (2026-06-28)

**Ambiguities found**: 6 (all decided by the user via the coordinator relay; this agent ran
as a subagent without `AskUserQuestion`). **Decisions reached the user in relay mode** — answers
relayed back through the coordinator and treated as the working spec, not direct user
confirmation. The "GEPA generalises to a capable online model" hypothesis and BigPickle's
6-instance-T3 headroom both stay **[lit-only]** until 27.2/27.3 close them locally.

**Code verification (against `scripts/harness_eval.py`, before asking):**
- ✅ `gepa_assert_serving_offline` at **line 1581** (plan cited 1581 — exact); forbidden keys
  `GEPA_REFLECTOR_FORBIDDEN_KEYS = {external_provider, model_ref, base_url}` at **1577**;
  text keys `GEPA_REFLECTOR_TEXT_KEYS` at **1574**.
- ✅ `online_preflight` at **306**, `apply_levers` external-provider branch at **414** (def 384),
  `score_instance` external path at **990**, `cmd_run` at **2356** (external block 2379-2389),
  `gepa_t3_shaped_score` at **1438**, `gepa_budget` at **1675**, `gepa_t3_gate_check` at **1551**.
- ✅ 6-instance tier-3 set in `harness_eval_subset.json` = 21614/12481/21627/22714/18621/15346;
  `online-bigpickle.json` present (external_provider, `opencode/big-pickle`, temp 0.0, timeout 240).
- ⚠ **Minor mislabel:** the plan's "builds-on" note attributes the health-check/`detect_model`
  skip to `cmd_run`; that logic actually lives in `score_instance`/`_score_subset` (990/2456),
  with only the startup notice in `cmd_run`. Line numbers correct, function attribution loose.
- ⚠ **Constant drift:** `GEPA_T3_SHAPED_CEILING = 0.50` (line 1434) — the plan said "reuse the
  19.2/23.1 unlock rule"; reusing the 0.50 ceiling verbatim is a measurement bug for a capable
  optimisee (see row 2).

| Category | Finding | Resolution |
|---|---|---|
| Constraint compliance / Integration wiring (guard not enforced) | `gepa_assert_serving_offline` is invoked **only in selftest** (lines 3061/3067/3222/3240) — `cmd_run` never calls it, and there is **no in-repo GEPA loop driver** (candidates are hand-authored `gepa-t3-*.json` configs run via `harness_eval.py run`). The 19/23 "asserted on every candidate" claim is enforced only by the manual driver, not by code, so 27.1 had no call-site to branch. | **Wire BOTH guards into `cmd_run`** (relay-chosen), mode-selected — every real candidate eval is checked. This **also retro-hardens the offline path** for items 19/23/25 (behaviour unchanged, invariant now enforced). Bigger change accepted. |
| Measurement & signal (capability-bound ceiling) | `gepa_t3_gate_check` hard-codes `GEPA_T3_SHAPED_CEILING = 0.50` — the *behavioural* ceiling for the 4B that cannot land F2P flips. A capable optimisee reaches the **+1.0 F2P rung**, so its shaped mean may exceed 0.50 → `(0.50 − mean) > spread` goes negative and **wrongly gates a climbable signal**. | **Online mode uses ceiling = 1.0** (binary F2P-flip); **local runs keep 0.50** (relay-chosen). Parameterise the ceiling by mode. `GEPA_T3_ADOPT_CEILING = 1.0` unchanged for both. |
| Scope gaps / Evidence policy (unmeasured baseline) | The plan states BigPickle "has real T3 headroom — 4/8 — so the binary F2P flip is a live adopt gate." But the 4/8 (item 22.3) is the **mixed 8-instance** subset (T3+T4) at 240s — **NOT** the 6-instance **tier-3-only** fitness set, on which BigPickle has **no recorded baseline**. | Tagged **[lit-only]** in the rewrite; **27.2 must measure the 6-instance-T3 baseline first** before the rung/ceiling choice is final (relay-chosen, weighted to measurement validity). |
| Integration wiring (mode selector) | The plan floated `--online-optimisee` / `optimisee_mode: online` as the opt-in, but item 22 already drives all online serve behaviour off `external_provider: true` — two sources of truth could disagree. | **Reuse `external_provider: true` as the sole selector** (relay-chosen); no new flag/field. Mode is set by the base config; the reflector cannot change the frozen key, so the mode is unambiguous per run, and a local run smuggling `external_provider`/`model_ref`/`base_url` still **fails loudly** under the offline guard (lines 1589-1595). |
| Cost & feasibility (budget dimension) | `gepa_budget` is pure wall-clock (`per_rollout_s × n × k`); the plan wanted "per-token cost + rate-limit", but the default optimisee (BigPickle) is **free** — token-cost is moot for the worked example; network variance / rate-limits are the real online risk. | **Add latency + rate-limit/retry-count + network-variance handling now**; **token-cost = recorded-but-zero** for the free default; a real per-token gateway-cost ceiling is **[lit-only]** until a *paid* `model_ref` is run (relay-chosen). |
| Constraint compliance (contamination) | With both reflector (Opus-4.8) and optimisee online, the same gateway model could reflect on its own traces. Default Opus-vs-BigPickle is disjoint, so risk is low. | **Warn + record** reflector identity + optimisee `model_ref` in the ledger `notes` when they share a family, but **allow** the run (relay-chosen). Hard optimisee==reflector ban rejected as too rigid. |

**Tasks added**: none net-new beyond 27.1-27.4 (which already existed as a spec); rewrote 27.1 to add the **`cmd_run` guard-wiring + offline retro-harden** clause and selftest coverage of the per-mode dispatch; rewrote 27.2 to **measure the missing 6-instance-T3 baseline** and apply ceiling=1.0; rewrote 27.3 to assert the guard via `cmd_run` and record the contamination warning; tightened the final `make check` task to "mode-selected guard asserted in `cmd_run`".
**Tasks removed/changed**: "Design decisions (to resolve — initial spec)" → "(resolved — 2026-06-28)"; opt-in **flag/field selector → reuse `external_provider`**; **unlock ceiling 0.50 → 1.0 for online mode**; budget "latency AND token-cost" → **latency + rate-limit/network-variance now, token-cost recorded-as-zero/[lit-only]**; contamination "record it" → **warn + record, no hard ban**; the "4/8 real headroom" framing flagged **[lit-only]** pending 27.2.
**Documentation changes**: `docs/structured-optimisation-research.md` (update — §27: `external_provider` mode selector, `cmd_run` guard wiring + offline retro-hardening, the 0.50→1.0 online ceiling, text transfer from the local 4B); `docs/opencode-local.md` (update — online-GEPA mode + constraint reframing: analysis-loop capability, never the frozen serve path); `CHANGELOG.md` (update — on close only).
**Key design constraint**: Item 27 generalises the GEPA loop to an **online optimisee** (BigPickle default, any `external_provider`+`model_ref` configurable) as an **analysis/optimisation-loop capability that never touches the frozen local serve path** — the offline-at-serve constraint binds the shipping harness, not this item. The mode is selected solely by **`external_provider: true`** (no new flag); **both guards are wired into `cmd_run`** so every candidate is checked (which also retro-hardens items 19/23/25, where the offline guard was previously only exercised in selftest). The climbing signal is the **shaped-T3 mean over the 6-instance tier-3 set** with the **unlock ceiling raised 0.50→1.0 for online mode** (a capable model reaches the F2P-flip rung, so the 4B's behavioural 0.50 cap would mis-gate it); the **adopt gate stays a binary F2P flip surviving an independent re-val**. The budgeter gains a **latency + rate-limit/network-variance** dimension now, with **token-cost recorded-as-zero** (free gateway) and a real paid-ref cost ceiling **[lit-only]**. Reflector/optimisee contamination is **warned-and-recorded, not banned**. Only the **TEXT levers** evolve (serve fields frozen via `GEPA_REFLECTOR_FORBIDDEN_KEYS`); whether item-19's "terse wins" survives on a capable model is the headline finding. The generalisation hypothesis and the 6-instance-T3 headroom both stay **[lit-only]** until 27.2/27.3 close them. Decisions relayed via the coordinator (2026-06-28); not direct user confirmation.

---

## TODO item 25 — GEPA-optimise the multi-agent PLANNING prompt: add an online-first (BigPickle) stage (2026-06-28)

**Ambiguities found**: 7 (all decided by the user via the coordinator relay; this agent ran
as a subagent without `AskUserQuestion`). **Decisions reached the user in relay mode** — answers
relayed back through the coordinator and treated as the working spec, not direct user
confirmation. The "GEPA-on-planning works mechanically / transfers across models" hypotheses
stay **[lit-only]**; the online Stage-1 result is explicitly **[online-probe]** and adopts nothing.

**Code/spec verification (against `scripts/harness_eval.py` + `TODO.md`, before asking):**
- ✅ Item-27.1 online-optimisee machinery is **DONE** and reusable by item 25's new Stage 1:
  `gepa_assert_online_optimisee` (1617) + `gepa_assert_optimisee_mode` dispatcher (1658) wired
  into `cmd_run` (1668-1671); the 0.50→1.0 online ceiling via `gepa_t3_gate_check(..., online=)`
  (1565); the online `gepa_budget` latency/retry dimension. `gepa_assert_serving_offline` (1590).
- ✅ `external_provider: true` is the sole online-mode selector (item 22/27), so a config with it
  set automatically **bypasses** the offline guard and takes the online guard — no new code/flag,
  no blocker for the online phase.
- ✅ BigPickle = `opencode/big-pickle` (item 22, `online-bigpickle.json`, temp 0.0, timeout 240);
  item-22's 4/8 was the **mixed 8-instance T3+T4** subset at 240s, **NOT** the 6-instance tier-3
  set item 25 uses → Stage 1 must measure its own arm-c baseline.
- ✅ `plan-arm-c-multiagent` (item 20.2) is the topology base; its `opencode_config` re-enables
  `task` + defines `planner`/`coder` subagents, carries no `mlx-local` block (so an online variant
  passes `gepa_assert_online_optimisee`).

| Category | Finding | Resolution |
|---|---|---|
| Experiment design / Cost & feasibility (staging) | Item 25 ran straight to the expensive/OOM-bound local Gemma GEPA loop without first checking whether optimising the planner prompt works *mechanically* on any model. | **Add Stage 1 = an ONLINE BigPickle GEPA-on-planning probe FIRST** (relay-chosen), cheap/fast (240s gateway) vs local ~455 s/OOM rollouts; it separates "does this work at all" from "does it transfer to the 4B". Local phases renumbered 25.2–25.4 behind it. |
| Adopt/reject criterion (greenlight gate) | No rule decided whether/when the local phases run, or what counts as Stage-1 success. | **BOTH signals required (strictest):** the `task` tool must **ACTUALLY FIRE** AND the shaped-T3/F2P signal must **move past spread at ceiling 1.0** on BigPickle. Fail either → **close item 25 without spending local budget** (a fourth valid closed negative). |
| Measurement & signal (baseline) | Item-22's 4/8 is the wrong reference (mixed 8-instance, bare config); item-27.2's pending baseline is generic, not arm-c. | **Same 6-instance T3 set, with a NEW BigPickle-on-`plan-arm-c-multiagent` baseline measured INSIDE Stage 1 (25.1b)** — its own reference; does NOT reuse 27.2's and does NOT block on it. |
| Integration wiring (item-27 dependency) | Whether item 25's online phase depends on / folds into item 27 (online-optimisee GEPA, 27.2-27.4 open). | **REUSE item-27.1's shipped machinery** (guard dispatcher, 1.0 ceiling, online budget — all DONE) but run **independently of 27.2-27.4**; item 25 is planning-specific (arm-c topology + planner/coder text). Overlap noted in docs so neither double-pays. |
| Constraint compliance / Evidence policy (online result status) | An online win could be mistaken for a ship/adopt decision, violating the offline-at-serve + local-validation discipline. | **[online-probe] — NEVER adopts into the shipped local harness**; it may greenlight Stage 2 and **seed the local reflector**, but only the local re-val (25.4) governs adoption. |
| Experiment design (counter-arm) | No negative-claim control on the capable model. | **Build a fixed naive planner-edit counter-arm vs the arm-c base on BigPickle** in Stage 1, so "GEPA-on-planning doesn't help even a capable model" is measured, not assumed. |
| Scope gaps (cross-model transfer) | Whether Gemma inherits BigPickle's optimal planner prompt was unspecified. | **Per-model prompt store + EXPLICIT Gemma transfer-verification:** keep separate BigPickle-optimal and Gemma-optimal planner texts; Gemma may **seed** from BigPickle's prompt but transfer is **verified, not assumed** — Stage 2 (25.3) runs Gemma-from-scratch vs Gemma-seeded-from-BigPickle and the adopted Gemma lever is whichever survives re-val. |

**Tasks added**: a `### Design decisions (resolved — 2026-06-28)` block (8 decisions); a `#### Stage 1 — ONLINE BigPickle feasibility probe` section with **25.1a** (build `plan-arm-c-multiagent-online.json` + feasibility smoke that `task` can fire under the online provider), **25.1b** (measure the own BigPickle-on-arm-c 6-instance-T3 baseline + budget at ceiling 1.0), **25.1c** (run GEPA-on-planning against BigPickle + counter-arm + per-candidate `task`-fired flag, emit a per-model BigPickle-optimal artifact), **25.1d** (the BOTH-signals greenlight/close-early decision); a `#### Stage 2 — LOCAL Gemma run` section renumbering the old phases and adding the **transfer-verification arm** (from-scratch vs seeded) to 25.3; expanded the measurement plan into Stage-1/Stage-2/per-model-artifact-store subsections.
**Tasks removed/changed**: old 25.1/25.2/25.3 → renumbered 25.2/25.3/25.4 (local, now gated behind Stage 1); old single "local-only" framing → two-stage online-first; the Evidence-policy outcomes gained a **fourth** (Stage-1 fails greenlight → close early); 25.3 gained the two-arm transfer comparison + a Gemma-optimal per-model artifact; 25.4 adopt rule pinned to the **Gemma-local** winner (never BigPickle's text unverified).
**Documentation changes**: `docs/structured-optimisation-research.md` (update — §25 covering the online-first staging, the both-signals greenlight gate, the per-model prompt store + transfer-verification, combining items 19+20+27); `docs/orchestration-planning-research.md` (update — whether the evolved prompt makes `task` FIRE on BigPickle vs Gemma + cross-model transfer); `docs/opencode-local.md` + `CHANGELOG.md` (update — on close only).
**Key design constraint**: Item 25 is now **two-stage, online-first**: **Stage 1** runs the GEPA-on-planning loop against the **capable online BigPickle optimisee** (reusing item-27.1's `external_provider` online-optimisee machinery, independent of 27.2-27.4) on its **own** newly-measured BigPickle-on-`plan-arm-c-multiagent` 6-instance-T3 baseline at the **1.0 online ceiling**, with a **fixed-naive-edit counter-arm**; it **greenlights Stage 2 iff BOTH the `task` tool actually fires AND the shaped/F2P signal clears spread** — failing either **closes item 25 early without spending the OOM-bound local Gemma budget** (a valid closed negative). The Stage-1 result is **[online-probe] and adopts nothing** — it only greenlights Stage 2 and **seeds** the local reflector. **Stage 2** is the local Gemma GEPA run (offline guard on every candidate) that **explicitly verifies cross-model transfer** (Gemma-from-scratch vs Gemma-seeded-from-BigPickle-optimal), keeps **per-model planner-prompt artifacts** (separate BigPickle-optimal and Gemma-optimal texts — transfer is verified, never assumed), and **only the local re-val (25.4) governs adoption** of the Gemma-local winner. Topology is fixed at arm-c multi-agent; only the planner/coder/orchestration TEXT levers (APPEND/sub-agent, never `system_prompt` REPLACE) evolve. Decisions relayed via the coordinator (2026-06-28); not direct user confirmation.

---

## TODO item 24 — Small-model survey: 4–7B local models vs the Gemma-4-E4B QAT baseline (2026-06-28)

**Ambiguities found**: 7 (5 listed open in the item + 2 integration gaps surfaced by reading
the serve path). Decisions relayed via the coordinator (relay mode — this agent ran as a
subagent without `AskUserQuestion`); answers treated as the working spec, not direct user
confirmation. The 24.1 v2 shortlist stays **[lit-only]** — only the 24.3 local K≥3 run adopts/rejects.

**Code/serve-path verification (before asking):**
- ✅ Local model selection is at the **serve layer**, not in the lever config: `harness_eval.py`
  does `served = detect_model(args.base_url)` then `model_ref = mlx-local/<served-path>`
  (2516-2517); `mlx.sh` picks the model via `MLX_MODEL`/`MLX_REVISION` env (29-31, 67-72) and the
  served id is the on-disk weights path → a `model-<name>.json` carries sampling/limits/rules ONLY.
- ✅ `mlx_repair_proxy.py` is **Gemma-4-token-specific** (ports `gemma4.py`: `<|tool_call>` /
  `<|"|>` / Gemma `enable_thinking` kwarg) — its repair is a no-op/confound on Qwen/Phi output.
- ✅ `mlx.sh` serves only `uvx --from mlx-lm==0.31.3 mlx_lm.server` (436) — no `mlx_vlm` path
  exists; the v2 survey flags Qwen3.5 small models as multimodal, possibly `mlx_vlm`-only.
- ✅ Scoring regime is already constrained by the item text + items 17/23 — confirmed, not re-asked.

| Category | Finding | Resolution |
|---|---|---|
| Integration wiring (engine freeze) | Item relaxes the *model* freeze but is silent on the *engine* freeze; Qwen3.5 4-bit builds may need `mlx_vlm`, which `mlx.sh` cannot serve. | **mlx-lm 0.31.3 FIRST; `mlx_vlm` is a GATED FALLBACK.** A candidate that won't load on mlx-lm is a recorded feasibility null; mlx_vlm bring-up happens ONLY if BOTH Qwen3.5 arms null on mlx-lm. Serving substrate stays constant by default. (Q1) |
| Tool-call reliability / Constraint compliance (proxy) | "Repair proxy stays ON" was written for Gemma; with repair-on it's a no-op/garble risk on non-Gemma tool calls and a confound on the tool-call floor. | **Passthrough (`MLX_PROXY_REPAIR=0`) for non-Gemma** — proxy in path for tracing, repair off, measure tool-call validity on the model's native mlx-lm parser. Per-model repair shim only if a systematic, fixable defect shows in the 24.2 smoke. "Proxy stays ON" reinterpreted as "in path, repair-mode passthrough for non-Gemma". (Q2) |
| Scope gaps (candidate axis) | Coding-specialised vs general-but-strong was open. | **General-but-strong, current-gen only:** all coding-specialised picks superseded/off-budget → funded shortlist = **Qwen3.5-4B then Qwen3.5-9B** vs the Gemma-4-E4B QAT baseline. **Phi-4-Mini NOT funded** (recorded maybe/null). (Q1/Q3) |
| Measurement & signal (quant parity) | Hold all candidates to ~4-bit, or best-fit per model with bpw as covariate? | **Best-fit MLX quant per model; effective bpw recorded as a covariate, NOT held constant.** QAT(Gemma)-vs-PTQ(candidates) method difference accepted as recorded; **any negative verdict flagged with the quant-method confound**. (Q4) |
| Cost & feasibility (budget) | One-model + slow OOM-bound T3 × K × N is expensive; N, K, scoring depth, wall-clock ceiling undecided. | **Full battery up front** (full pass/8 + shaped-T3 + T1/T2 micro), K≥3, 2 candidates, sequential serve/teardown, per-candidate wall-clock ceiling (item-23 `gepa_budget`) + abort. **Qwen3.5-9B flagged as an explicit OOM risk** → null if it OOMs, don't chase. No cheap-gate-first sequencing. (Q3) |
| Definition of done (24.2 deliverable) | 24.2 deliverable was "a config JSON per candidate" — but the model is chosen at serve time, not in the config. | **24.2 deliverable is a per-candidate SERVE RECIPE** (env + `make mlx-pull` + revision pin into `mlx-models/`) + a sampling/limits/rules `model-<name>.json` + the passthrough-proxy setting; the config does NOT select the model. |
| Measurement & signal (scoring regime) | Item proposed reusing the item-17/23 tiered harness "(confirm in plan-review)". | **Confirmed unchanged** — T1/T2 micro + 6-instance shaped-T3 + full pass/8, single lever = served model, no new scoring code (served model auto-detected). |

**Tasks added**: rewrote the "Design decisions" block from 5 open questions → 7 resolved
decisions (added the engine-scope ruling + the proxy-mode ruling as explicit entries); folded
the per-candidate **serve-recipe** deliverable, the **passthrough-proxy** setting, the
**mlx-lm-first / mlx_vlm-gated-fallback** null rule, and the **OOM-risk-9B** flag into the 24.2
and 24.3 bullets; added the proxy-repair-mode nuance to the measurement plan's "single lever" note.
**Tasks removed/changed**: 24.2 "config JSON pointing at the candidate" → "serve recipe (env/pull/
revision) + sampling-only config + passthrough proxy"; 24.3 "evaluate on the same tiered harness,
K≥3" → "FULL battery up front, K≥3, wall-clock ceiling, 9B OOM-null discipline, quant-confound flag
on negatives"; Phi-4-Mini demoted from shortlist candidate to unfunded recorded-maybe.
**Documentation changes**: `docs/small-model-selection-research-v2.md` (current shortlist, already
delivered — no change needed); `docs/opencode-local.md` (update — record item 24's adopt/reject
outcome on 24.3 close, AND document the non-Gemma passthrough-proxy + mlx-lm-first/mlx_vlm-fallback
serve rulings); `CHANGELOG.md` (update — on close only). [no new doc created this pass]
**Key design constraint**: Item 24's funded A/B is **two current-gen candidates — Qwen3.5-4B then
Qwen3.5-9B — served ONE AT A TIME on the frozen mlx-lm 0.31.3 path** (`mlx_vlm` only as a gated
fallback if BOTH null), each at its **best-fitting quant with bpw as a recorded covariate**, with the
**Gemma-token repair proxy switched to passthrough** for these non-Gemma models (tool-call validity
read off each model's native parser), scored on the **full item-17/23 tiered battery at K≥3 up front**
against the recorded Gemma-4-E4B QAT baseline; the 9B is an explicit OOM-null risk, any negative verdict
carries a quant-method-confound flag, and **only the local run adopts/rejects** ([lit-only] survey ranks
order only). Decisions relayed via the coordinator (2026-06-28); not direct user confirmation.
